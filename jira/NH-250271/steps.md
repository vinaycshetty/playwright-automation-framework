Manual steps for crawler run

- Purpose: I will log in manually in the headed browser when it opens. After login and navigating to the relevant pages, return to the terminal and press Enter to allow the crawler to continue.

Instructions:

1. The crawler will open a headed browser and navigate to the URL provided.
2. Manually perform login using your test credentials.
3. Navigate through the pages you want locators captured for.
4. When ready, switch back to the terminal and press Enter to let the crawler proceed.

Notes:

- Do NOT include actual passwords here. The crawler will wait for you to log in manually.
- If you want the crawler to auto-login, rerun with a credentials file path or provide username/password (not recommended here).
